# Memory

<!-- Loaded every session. Keep lean — index only, deep knowledge in topic files.
     END-OF-SESSION: review gotcha-log, promote patterns, retire stale entries. -->

## Topic Files

| File | When to load | Key insight |
|------|-------------|-------------|
| `memory/gotcha-log.md` | Stuck or debugging | Problem-fix archive |
| `memory/data-formats.md` | Working with energyDataHub data | **Schema v2.2 envelope** (`{metadata, data:{...}}`, since 2026-06-07) wrapping the v2.1 inner shape; units, timezone conventions; and the rule that coverage is never read from metadata |
| `memory/ml-decisions.md` | ML architecture choices | Historical XGBoost plan + River ARF era; superseded by ADR-006. **ARCHIVAL — frozen by design, last edited 2026-05-29.** Its age is correct, not drift: it records what was decided in the ARF era and must not be updated to match the present. `/curate`'s 30-day staleness check will keep flagging it; that flag is expected and should be dismissed, not fixed. Live ML state is this index plus `docs/experiment-results.md`. |

## Current State

- Dashboard: 5 tabs (Prices, Weather, Grid, Market, Model) on Netlify, served from `static/data/augur_forecast_shadow.json` (price charts) and `static/data/augur_forecast.json` (Model-tab metrics). **Chart time-axis convention is now enforced at a single rendering boundary** — `utcToLocalNaiveISO()` in `static/js/modules/chart-renderer.js` normalises every timestamp to browser-local naive ISO at `renderChart()`/`getCurrentTimeLineShape()`/`getChartLayout()`. ADR-008 documented this convention; commit `fc24676` (2026-06-06) is its actual implementation. Data layer keeps using `new Date(ts)` for sorting/filtering.
- Weather tab: two-dropdown UI parity. Both "Temperature & Wind (10-day)" and "Cloud Cover & Humidity" each show a `<select>` (synced — changing one mirrors the other before re-render). Distinct aria-labels per chart. Commits `656e917` + `fa28450`.
- **EDH v2.2 envelope shim, both sides** (`4a557c8` JS 2026-06-07; `e11487b` Python 2026-06-10): EDH commit `3dfc7fb` (2026-06-07 12:43 CEST) wrapped `energy_price_forecast.json` + `wind_forecast.json` under `{metadata, data: {...}}` to match the other 14 strategic feeds. The dashboard JS got a defensive `obj.data ?? obj` shim in `4a557c8` (data-processor.js:117, tab-charts.js:64-79, dashboard.js:264). The Python parsers ALSO needed the equivalent shim — the 4a557c8 commit message asserted "Python migrated transparently via `_migrate_2_1_to_2_2`" but `load_json_file` in `ml/data/consolidate.py` never invokes schema_registry, so parsers silently returned empty Series for every v2.2 file until `e11487b` added `_unwrap_v22_envelope` at three sites (`parse_price_file`, `_parse_single_source`, `parse_wind_file`). Pattern updated in the gotcha-log Promoted table to reflect (a user-memory file was named here until 2026-08-29 and never existed — `/curate` flagged it as a dead reference): NEITHER side auto-migrates; audit JS + Python parsers + any direct `json.load`/`fetch` of EDH files on every schema bump.
<!-- verify: [ -f static/data/augur_forecast.json ] && [ -f static/data/augur_forecast_shadow.json ] && [ -f layouts/index.html ] && grep -q "utcToLocalNaiveISO" static/js/modules/chart-renderer.js && grep -q "weather-location-cloud" static/js/dashboard.js && grep -q "energyData.data ?? energyData" static/js/modules/data-processor.js && grep -q "_unwrap_v22_envelope" ml/data/consolidate.py && echo PASS || echo FAIL -->
- ML pipeline: **live** — LightGBM-Quantile in production (ADR-006), ARF as backup signal (ADR-004 superseded). Daily system-level systemd timer on sadalsuud (fires 16:30 UTC + `wait_for_edh.sh` ExecStartPre gate, post-augur#12) runs ARF + parquet consolidate + LightGBM retrain + shadow eval.
<!-- verify: grep -q "ml.shadow.update_shadow" scripts/daily_update.sh && grep -q "augur_forecast_shadow.json" static/js/dashboard.js && echo PASS || echo FAIL -->
- **LightGBM promotion**: EXP-014 (2026-05-29), redesigned criterion (skill DM p<0.10, calibration not >0.02 worse than ARF). LGBM MAE 28.9 vs ARF MAE 38.4 (25% better, DM p=0.029). One-line revert path: change `static/js/dashboard.js:loadAugurForecast` back to `augur_forecast.json`.
- **ARF retained as backup signal**: still runs daily inside `daily_update.sh` (now systemd-triggered post-augur#12, not cron); produces `augur_forecast.json`, the surcharge cache in `ml/models/state.json` (consumed by LightGBM's consumer-pricing step), and the timestamped archives in `ml/forecasts/` used by `evaluate_shadow.py`. Retiring ARF infrastructure deferred until ≥1 rolling-window cycle (~56 days) of clean LightGBM operation.
- **Pipeline reliability guard (2026-08-28, `4a2afc4` + `05b4d43`, deployed to sadalsuud)**: t0 is data-derived (`parquet.index.max()`), so a stale parquet stalled it — overwriting a vintage via the `(timestamp_utc, eval_day)` dedup — and a catching-up parquet skipped one permanently. `classify_t0_advance` now alarms in the daily commit subject on any advance but +1 calendar day. **The publish-hour floor it shipped beside is gone**: `MIN_PUBLISH_HOUR_UTC=12` was replaced 2026-09-01 (`7d0066d`) by a **content contract** — the report must be strictly newer than the last consumed *and* the primary dataset (`entsoe`) at least the median point count of the last N publishes, polling to a 03:00 UTC deadline. GitHub defers EDH's cron unpredictably (observed 00:18–21:14 UTC), so a clock floor shut the gate on good publishes; deriving the expectation also absorbs an upstream resolution change. **Second occurrence of this exact mechanism** — diagnosed 2026-06-12 (vintages 06-08/06-10) and left unguarded; the alarm shipped then was downstream and named the wrong component. Permanent `eval_log.jsonl` holes: 2026-06-08, 2026-06-10, 2026-08-25.
<!-- verify: grep -q "classify_t0_advance" ml/shadow/update_shadow.py && grep -q "T0_MARKER" scripts/daily_update.sh && grep -q "median_points" scripts/wait_for_edh.sh && ! grep -q "MIN_PUBLISH_HOUR_UTC" scripts/wait_for_edh.sh && echo "guard, marker and content-contract gate present; clock floor gone" || exit 1 -->
- **PRODUCTION INCIDENT 2026-08-30/31 — model down one night, fixed (`87ed30c`)**: the shadow update crashed (`No clean feature row at t0`), no forecast written, dashboard stale ~24h, **2026-08-30 vintage permanently lost**. Cause **(corrected 2026-08-31 by the EDH session)**: not an EDH collector change and not a deliberate horizon split — the request window was unchanged at 48h and the envelope still declared full coverage; **ENTSO-E returned A44 prices for 08-31 but not A65 load**, outage residue after a total 503 on 08-29. Transient, upstream of EDH. Augur's exposure was structural: and `t0 = price.dropna().index.max()` followed the *longest* column while `predict_72h` needs *all five*. Fix: `latest_feasible_t0` anchors on the last **complete** feature row and names the short feed in the alarm. **The first version of that fix repeated the very lesson the incident taught** — it only *logged* the hold-back, so a degraded run would still commit a clean `shadow rc=0`; `t0_held_back_hours`/`t0_short_feeds` now go into state and out as `[ALARM: t0 held back Nh — <feeds> short]`, which the heartbeat reads. Self-clears when upstream restores. Live anchor moved 08-31 21:00 → 08-31 03:00 (held back 18h), row clean, deployed + verified. **Does NOT recover the lost vintage** (`t0 jumped 2d` still fires, correctly) and **does NOT touch the feature set** — dropping `load_forecast` is EXP-018a's call ~09-07 and an incident fix must not pre-empt a pre-commit. **Upstream load-feed truncation filed as energydatahub#51** (single-publish observation, caveated as possibly transient; related to #33 which covers both feeds narrowing, whereas #51 is about them *diverging*). ⚠️ **The "possibly transient" caveat is SUPERSEDED as of 2026-09-04** — four consecutive short publishes measured, and the "transient outage residue" reading refuted; see the `load_forecast` horizon bullet below and `docs/hypothesis-log.md` [2026-08-31]. This sentence is kept as the record of what was believed on 08-31, not as a live position. Schedule cost: one vintage, making EXP-018a's "only if no day is missed" caveat materially more binding.
<!-- verify: grep -q "latest_feasible_t0" ml/shadow/update_shadow.py && grep -q "TestLatestFeasibleT0" tests/test_update_shadow.py && echo PASS || { echo FAIL; exit 1; } -->
- **The alerting missed it, one day after shipping — third failure mode (`da57139`)**: the run **completed, committed, exited 0** with `shadow rc=1` in the subject, so `OnFailure=` did not fire (unit succeeded) and the heartbeat read healthy (commit landed, 10h old, timer fine). Every soft-failure alarm this pipeline emits rides in the **commit subject** and nothing read them. Heartbeat now greps the newest daily subject for `[ALARM: ...]`, `ARF FAIL`, non-zero step rc, `rc=skip`. Also: `augur-daily.service` sets `StandardOutput=append:` which **overrides journal capture**, so `journalctl -u` holds only lifecycle lines and the first alert email shipped zero diagnostics — the email now leads with 40 lines of `logs/daily_update.log`, and the unit's misleading comment is corrected (`c9c113e`). Lesson: scope alerting to the failure modes the pipeline can *express*, not to the one that motivated the work.
- **Failure alerting built + deployed + verified 2026-08-30 (`6d434a2`)**: two units, because each is blind to the other's case. `augur-daily.service.d/alert.conf` adds `OnFailure=augur-alert@%n.service` → `scripts/alert_failure.sh` for a unit that **dies** — the whole gap for hard failures, since every in-script ALARM rides in the *commit subject* (no commit, no signal); that is the 2026-06-29→07-03 shape, five silent nights. `augur-heartbeat.timer` (06:00 UTC → `scripts/heartbeat_check.sh`) covers what `OnFailure=` **structurally cannot fire for**: `daily_update.sh` ends `git diff --cached --quiet && echo "No changes to commit" || {commit; push;}`, so a run producing nothing exits 0 and the unit *succeeds* — likewise a disabled timer or a box down at 16:30 UTC. Alarms on no `Daily update` commit in >30h / timer not enabled+active / unpushed commits. Channel is FluxusSource's `secrets.ini` Gmail sender via `scripts/notify_email.py` — the same one `nexusmind-alert@.service` uses; **no new notification service** (user's call 2026-07-17). Accepted blind spot: it runs *on* sadalsuud, so it cannot report sadalsuud being down. Install + rollback in `scripts/systemd/README.md`. Verified on the box: `OnFailure=augur-alert@augur-daily.service.service` shows on the daily unit, `augur-heartbeat.timer` enabled+active (next 06:00 UTC), a manual alert fire delivered a real email, the healthy heartbeat logged `heartbeat OK` without emailing, and a second fire confirmed the burst guard suppresses.
<!-- verify: [ -f scripts/notify_email.py ] && [ -f scripts/alert_failure.sh ] && [ -f scripts/heartbeat_check.sh ] && grep -q "OnFailure=augur-alert" scripts/systemd/augur-daily.service.d/alert.conf && echo PASS || { echo FAIL; exit 1; } -->
- **Upstream residual — EDH skips whole days**: published on 31 of 35 days over 2026-07-25..08-28 (missed 08-03, 08-06, 08-23, 08-27; catch-up double-publishes 08-09, 08-24), roughly an 11% miss rate. Augur's gate is fail-open by design, so each miss costs a vintage and now announces itself. Filed as **energydatahub#50**.
<!-- verify: n=$(gh api "repos/ducroq/energyDataHub/commits?path=data/data_quality_report.json&per_page=100&since=2026-07-25T00:00:00Z&until=2026-08-29T00:00:00Z" --jq '.[].commit.committer.date' 2>/dev/null | cut -c1-10 | sort -u | wc -l); if [ -z "$n" ] || [ "$n" = 0 ]; then echo "CANNOT VERIFY: gh api unreachable or empty"; else echo "EDH published on $n of 35 days in 2026-07-25..08-28 (claim says 31)"; [ "$n" = 31 ] || exit 1; fi -->
- **EDH PUBLISH OUTAGE 2026-08-31..09-04 — five vintages lost, upstream, resolved (diagnosed 2026-09-04)**: EDH's "Collect and Publish Data" failed on every run from 08-31 to 09-03, last good publish 2026-08-30T19:03. Cause: its **schema-drift tripwire deadlocked**. The tripwire (`collect-data.yml:153`) runs *before* the `Update energy data` commit (`:183`) and compares against `--previous-ref HEAD`, but `data/_shape_signatures.json` is only written *by* that commit — so the baseline froze at 08-30 and the gate was guaranteed to re-fire, with a different offending feed each run as the gap widened. Only `Record shape observation` commits landed, which made the repo history look alive while publishing nothing. **Augur behaved correctly throughout**: the t0 guard, the hold-back alarm and the heartbeat all fired, the heartbeat emailed on day 1 and then deduplicated by shape, and on 09-04 the new content-contract gate **refused a pre-auction vintage** (dispatched 06:53 UTC, `entsoe` at 96 points vs a 192 median — the day-ahead auction clears ~12:00 CET). No corrupt data entered the parquet. **The fix is EDH-side and was theirs to make.** A time-boxed drift block (block at most 3 consecutive runs, then relent, baseline still only advancing on a pass) was implemented and then **abandoned, not shipped** — their review found its only reachable domain was `CRITICAL_FEEDS` + `MEMBER_MAPPED_FEEDS`, the two sets that must never be waved through, and that it would have relented on **zero of the five** runs this outage actually produced. What landed instead is **energydatahub PR #64, merged as `a5993e7`** — publish-failure alerting only, plus tests that run when `scripts/` changes and a manual self-check (`a96a7f7`). **NEW UPSTREAM SIGNAL — it is DIAGNOSIS, NOT DETECTION. Do not make it the primary alarm** (an earlier version of this bullet said "use it before inferring from our own alarms", which had the hierarchy backwards and is corrected here). A failing EDH publish opens an issue titled *"Daily publish is failing"* on `ducroq/energydatahub` carrying the run URL and both job results, comments rather than duplicating on repeat failures, and closes itself on recovery. But `alert:` is a **job inside `collect-data.yml`** with `needs: [collect-and-publish, deploy]`, and that workflow's only triggers are `schedule` and `workflow_dispatch` — verified on main. **So it is structurally blind to a run that never starts**: a dropped cron (GitHub deprioritises schedules on quiet repos), a queue outage, or a disabled workflow all produce no run, therefore no alert, therefore a clean issue tracker while nothing publishes. Correct division: **Augur's `t0` hold-back and heartbeat detect the ABSENCE of a fresh vintage; EDH's issue explains a run that fired and FAILED.** Ours covers the worse case. ⚠️ **Our alarm firing with NO issue on `ducroq/energydatahub` is a worse signal than an issue existing** — it means the pipeline is not running at all, rather than running and failing. For the 08-31..09-03 outage theirs would have fired on all five days; had the cron simply stopped, theirs would have been silent on all five and only ours would have caught it. A true liveness check must fire when a publish has *not* happened for N hours and **cannot live inside the workflow it watches** — unbuilt on both sides, and the same shape as Augur's own accepted blind spot (the heartbeat runs *on* sadalsuud so it cannot report sadalsuud down). Validated on a real runner with the write-scoped credential (the create path needs issues-write where the close path needs only read — a PAT sufficient for the daily run could still have been unable to raise the alarm, discoverable only mid-outage). ⚠️ **`wind_forecast` → `CRITICAL_FEEDS` did NOT ship** — I recorded it as part of #64 on 2026-09-04 and that was wrong; verified against the API, `CRITICAL_FEEDS` on main is still the original four. It sits committed-but-unmerged on branch `critical-feeds-wind-forecast`, held back deliberately: it makes the gate stricter (a shape excursion would block a publish where it now warns, ~1 run/month measured) and shipping something that can cost a publish while nobody is watching was the wrong trade. So the span/structure gap is documented and **open on both sides**, not quietly closed. ⚠️ **Issue-number hygiene (2026-09-04), and note the scope — this is about the TIME-BOX only:** the time-box was cited as `#47`, then as `#50`, and **neither is its number**; it has none, because it does not exist. Verified against the API: `#47` = member-drift duration guard, `#50` = skipped-publishes ("silently skips whole days"), `#51` = load-horizon. **`#50` remains a correct and live reference** — it is the issue this index has tracked since 08-28 *and* the one PR #64's publish-failure alerting addresses, so do not strike it anywhere. Citing any number for the abandoned time-box is how a phantom dependency gets recorded. ⚠️ **Do not "fix" this by moving Augur's gate or proposing EDH move theirs after the commit** — `collect-data.yml:119-121` states the invariant: the baseline "must only advance on a passing run, or a broken shape becomes the new normal". Moving it would have adopted SE-less `nordic_hydro` and 10-of-11-location `demand_weather_forecast` as normal, i.e. silent permanent data loss into Augur — strictly worse than a visible gap. **Schedule cost: every fresh-vintage-gated trigger slips ~5 days** — EXP-018a Stage 1 / EXP-021a from ≈09-09, and the feed-divergence review from ≈09-07.
- **`load_forecast` horizon halved 2026-08-28 and has not recovered — NOT structural (measured 2026-09-04)**: decrypted every `load_forecast` vintage on EDH `origin/main` and counted **distinct calendar days** as well as points, so a resolution change cannot be mistaken for a horizon change. Normal is **192 pts / 2 days** for months through 08-26; **96 pts / 1 day** on 08-28, 08-29, 08-30 and 09-04. `metadata.end_time` still declares +2 days throughout, so the envelope over-declares its own coverage and EDH's request window is unchanged — this is **upstream ENTSO-E A65 availability**. **RESOLVED 2026-09-04 on the load side — both zones recovered; see `docs/hypothesis-log.md` for the full table.** EDH's `260904_081356` publish (08:14 UTC) carries **load NL 192/2 days AND DE_LU 192/2 days**. A direct A65 probe 15 minutes earlier (09:59 CEST) had shown DE_LU still at 96, which **confirms the publication-timing effect** — German day-ahead load lands later in the day than Dutch, so a morning DE_LU short reading is benign. The 08-28..08-30 episode was nonetheless a **genuine both-zone A65 gap**, not timing: `260830_190255` was short at 19:02, far later than today's ~10:14 recovery. Net: a real multi-day upstream gap that healed, costing three vintages of reach — so **energydatahub#51 closes as resolved-upstream, not as transient**, and the "new steady state" reading is falsified *before its pre-committed signal could accumulate* (it needed load to stay at 96; load is back at 192). ⚠️ Price at 96 in both of today's publishes is **not** the divergence — both are pre-auction (the NL day-ahead clears ~12:00 CET). **This is an NL-only question for Augur**: `consolidate.py:320 parse_load_file` reads `load_data.get("NL", {})` hard-coded, and `grep -rn "DE_LU" ml/ static/js/ scripts/` returns zero hits repo-wide — DE_LU is structurally invisible here, so do not spend time on the German half. ⚠️ **Two wrong readings to not repeat, both held during the 09-04 session**: "load_forecast is a same-day feed, 96 quarter-hours by construction, so the 18h hold-back is permanent and should be designed around" (refuted — it carried 192 for months), and the mirror error of generalising a structural shape from a *single* vintage. The discriminator both readings missed is **publish hour**: every pre-08-28 short vintage is an early-hour run that recovered to 192 on the same day's normal-hour run (08-09, 08-24), so short-at-early-hour is benign and normal-hour-short is the signal. **`260830_190255` at 19:02 is the lone unambiguous normal-hour short vintage**, which is why the feed-divergence hypothesis's Alternative 1 ("new steady state", signal = 3 consecutive normal-hour publishes at price 192 / load 96) is **strongly indicated but formally short** — the outage denied us the normal-hour publishes the test needs. Resolve on the next two scheduled EDH runs. **energydatahub#51 must not be closed as transient on this evidence.** Standing gap, told to EDH and unowned by either side: `load_forecast` is in their `CRITICAL_FEEDS` and delivered half its horizon for four publishes without their gate objecting, because **a shape signature captures structure, not span** — 96 and 192 records have identical shape. Augur's t0 hold-back alarm was the only thing that noticed.
<!-- verify: grep -q "t0_short_feeds" ml/shadow/update_shadow.py && grep -q "t0 held back" scripts/daily_update.sh && echo "hold-back alarm still the only span detector on this side" || exit 1 -->
- **Known weakness in production — calibration (augur#19), reframed 2026-08-25**: the P80 band under-covers and the deficit has **moved to the upper side**. From `calibration_history` (NOT `eval_log.jsonl` — mixed vintages, permanent holes 06-08/06-10): Jul 2026 lower 0.865 / upper 0.841 / band 0.706; **Aug 2026 lower 0.887 / upper 0.774 / band 0.660** (target 0.80). The lower side has healed since the 0.834-through-06-11 figure that opened the issue. Likely driver: Aug mean price 128 EUR/MWh (highest in the parquet) — a trailing-56-day model under-reaches an upward level shift. **EXP-015 + EXP-016 (2026-06-12, both `parked`)** remain valid as replays but their conclusion ("gap lives in a high-biased q10_raw → EXP-017 9-quantile next") is **stale on its premise** — the live breach is the other side. Re-derive after the feature set settles. Replay scripts: `scripts/exp015_replay_cqr.py`, `scripts/exp016_replay_aci.py`. Standing finding: production `compute_cqr_q` conformalizes already-widened bands (feedback loop), not raw quantiles.
- **Model work 2026-08-25..30 — 13 experiments, EXP-018..EXP-033. Nothing shipped; no production path touched.** Full record: **`docs/experiment-results.md`**, which opens with a *Decision state* section (closed / live / standing evidence) derived from the registry so it cannot drift. Read that before proposing model work; the four settled conclusions are:
  1. **Feature engineering is closed at a 56-day window.** Added exogenous columns buy nothing and added *level* columns actively cost — four independent confirmations (EXP-019 rolling mean, EXP-020 TTF gas, EXP-024 matched-count derived columns, EXP-029 residual regression).
  2. **A zero-shot foundation model beats the tuned incumbent** by 20.3% QS / 16% MAE with no features at all (EXP-021). EXP-022 splits that ~12pp pretrained prior / ~8pp context volume, and EXP-024 showed the context half is *not* recoverable by widening LightGBM's feature row — the bottleneck is model class.
  3. **Do not fine-tune** (EXP-027: MAE +19%, coverage 0.825→0.394 — only ~3.6k pre-cutoff points). Zero-shot is the deployment mode. Size and latency are non-constraints (EXP-026/030/033: tiny keeps 93.5%; base runs 1.16s on sadalsuud, ~52× headroom).
  4. **Exogenous data is not useless — it was unusable *by LightGBM*.** Chronos-2 gains 8.3% QS from the same columns with no retraining (EXP-028), Augur's first positive exogenous result. Fails its coverage gate; `parked`.
<!-- verify: [ -f scripts/exp021_foundation_zeroshot.py ] && .venv/bin/python -c "import json;g=json.load(open('ml/shadow/exp021_foundation_aligned/summary.json'))['gates']['primary'];print('primary verdict',g['verdict']);raise SystemExit(0 if g['verdict'].startswith('SUPERIORITY') else 1)" -->
<!-- verify: [ -f scripts/exp020_fundamentals_ablation.py ] && [ -f ml/shadow/exp020_fundamentals/summary.json ] && [ -f ml/shadow/exp020_fundamentals_ctl/summary.json ] && .venv/bin/python -c "import json;d=json.load(open('ml/shadow/exp020_fundamentals/summary.json'));g=d['gates']['primary'];print('primary ALL_PASS',g['ALL_PASS']);raise SystemExit(0 if g['ALL_PASS'] is False else 1)" -->
<!-- verify: [ -f scripts/exp018_stage0_ablation.py ] && [ -f ml/shadow/exp018_stage0/summary.json ] && grep -q "EXP-018a" docs/hypothesis-log.md && echo PASS || { echo FAIL; exit 1; } -->
- **Two numbers NOT to requote, both corrected mid-arc:** the FM's calibration edge is **0.811 vs lean+CQR 0.788**, *not* against the pre-conformal 0.611 (EXP-025 — production ships CQR); and EXP-021's stated level-drift mechanism was **wrong** — it is a pretrained distributional prior plus a feature-row bottleneck (EXP-022). Both wrong versions are still in this session's transcript and early commits.
- **Live decisions, all gated on fresh vintages** (triggers + review-by dates in `docs/hypothesis-log.md`): **EXP-028a** ≈09-23 (covariates; also the only thing that resolves its contamination exposure), **EXP-021a** ≈09-09 (FM ladder; latency half discharged, tail-risk half open), **EXP-023a Stage B** ≈10-09 (112d window — **demoted**: Stage A/EXP-032 found it 1.4% *worse* on unscored vintages, sign flipped). **EXP-018a Stage 1 ≈09-09 has first claim** on that window and decides `lean` vs `full`.
- **Cross-project boundary, set 2026-08-31 — do not let these drift into each other.** A Minsky-framing conversation produced two *different* fragility ideas with different homes. **Augur = model-calibration fragility** (EXP-034: does the forecast know when its own bands are about to fail). **Aegis** (`ducroq/Aegis`, private — "Real-Time Macro Defense", four layers, live at aegis.jeroenveen.nl) **= macro/portfolio fragility**, and it is where the Minsky material belongs: the endogenous leverage/reflexivity mechanism that had to be caveated away for Augur actually applies there. Two questions were filed to its `Aegis/docs/OPEN_QUESTIONS.md` (commit `8268b30`; path qualified 2026-09-04 so `/curate`'s dead-reference check reads it as cross-repo rather than a missing local file): price-of-risk vs quantity-of-leverage in the credit block, and sustained calm as a positive-signed risk input. **Augur must not grow macro indicators**; Aegis must not grow forecast-calibration machinery.
- **Record integrity is checkable, not asserted:** `scripts/audit_registry.py` (schema, id order, artifacts, number-traceability, sha256 proof no pre-committed Method was edited post-hoc) and `scripts/render_results.py --check`; both exit non-zero. EXP-031 found and fixed 19 untraceable registry numbers.
- ENTSO-E collector recovered ~2026-04-18 after 2026-03-26 outage; guard in `parse_price_file()` remains.
- Test suite: **285 tests** collected (was recorded as 243 until 2026-09-04; the annotation below caught the drift — the intervening additions were never tallied here) (177 + 18 in `test_consolidate.py` + 7 in `TestClassifyT0Advance`, 2026-08-28, for the t0-advance guard + 20 in `test_consolidate.py` for the EXP-020 fundamentals parsers, 2026-08-29 + 12 in `test_notify_email.py`, 2026-08-30, for the alert channel's credential layer and never-throw exit contract + 7 in `TestLatestFeasibleT0`, 2026-08-31, for feasible-t0 selection + 2 in `TestHoldBackReachesState`, 2026-08-31, for the hold-back marker) (was 195 + 18 new in `test_consolidate.py` for EDH v2.1/v2.2 parser shapes, units, timezone normalisation, isinstance guards — added 2026-06-10 as code-review followup to the v2.2 fix).
<!-- verify: n=$(.venv/bin/python -m pytest tests/ --collect-only -q 2>&1 | grep -oE "[0-9]+ tests" | grep -oE "[0-9]+"); echo "collected ${n:-none}"; [ "$n" = 285 ] || exit 1 -->
- Experiment registry: EXP-001..EXP-033 in `experiments/registry.jsonl`, 32 lines (gap at EXP-017 — queued, never run). EXP-014 is the production-promotion entry; EXP-015/016 the parked calibration replays (2026-06-12); EXP-018 the feature ablation (`kept`, evidence); EXP-019 the refuted stationary reparameterisation (`rejected`); EXP-020 the refuted fundamentals bet (`rejected`, 2026-08-29); EXP-021 the foundation-model arm (`kept`, evidence — the only experiment since EXP-014 whose position was *exceeded*); EXP-022 the context-ladder mechanism diagnostic (`parked`); EXP-023 window sweep (`parked`, 112d wins), EXP-025 band transplant (`rejected` — but corrects the calibration claim), EXP-026 size ladder (`kept`), EXP-028 chronos-2 covariates (`parked` — first positive exogenous result), EXP-029 residual screen (`rejected` as a gate). Gaps at EXP-017/024/027, never run.
<!-- verify: n=$(wc -l < experiments/registry.jsonl); last=$(tail -1 experiments/registry.jsonl | .venv/bin/python -c "import json,sys; print(json.load(sys.stdin)['id'])"); echo "$n lines, last $last"; [ "$n" = 32 ] && [ "$last" = EXP-033 ] || exit 1 -->
- Daily-pipeline output guards (2026-06-12, `1c33daa`): two post-run checks in `daily_update.sh` alarm in the commit subject when the ARF forecast spans <24h or no eval row lands for >2 days — closes the "rc=0 but empty output" blindspot from the EDH v2.2 incident. **ARF forecasts currently 48h (not 72h)** — v2.2 files only span today+tomorrow; tracked as **augur#26**, alarm deliberately doesn't fire on it.
<!-- verify: grep -q "ARF_FC_HOURS" scripts/daily_update.sh && grep -q "EVAL_LAG_DAYS" scripts/daily_update.sh && echo PASS || echo FAIL -->
- **EXP-034 pre-committed 2026-08-31 (designed, not run)** — the one open backlog entry. Position: a **leading** scarcity signal (residual-load z from forecast columns, known at t0) breaks the **γ-independent ~0.85 ceiling** EXP-016 hit, because ACI reads only *past* coverage errors and therefore cannot react on the first day of a shift. **Not a re-run of EXP-020**: that refuted these columns as level features for the conditional *mean*; this tests them as conditioning variables for the *variance*, where the price response is convex. Three arms including a **matched-mean-width uniform control** (arm B) — without it the test only measures "wider bands cover more", the EXP-024 lesson applied to width. Queued **after EXP-018a Stage 1**, since the replay consumes production-model raws. Minutes of CPU, no fresh vintages.
- **Open judgement call deliberately left unfixed (2026-08-31)**: `T0_MARKER` in `daily_update.sh` is computed only `if [ "${SHADOW_UPDATE_RC:-1}" -eq 0 ]`, so a crashed shadow run **suppresses the t0 alarm that would name why**. The gate is correct in itself — on a failed run `shadow_state.json` still holds the previous run's values, and ungating it trades a suppressed-alarm blind spot for stale-value false alarms. Detection no longer depends on it (the heartbeat reads `rc=1` from the commit subject independently), so this is recorded rather than changed. Revisit at the t0-guard hypothesis review ≈2026-09-11.
- **A third parked branch, `wip/drop-epex-target` — reviewed and NOT merged 2026-09-04.** One commit (`f4387a7`, 2026-09-01, +258/−29 across `ml/data/consolidate.py` + `tests/test_consolidate.py`): drops `epex` from the training-target fallback chain. Its evidence is strong — measured against `entsoe` over the full published history, `epex` is the worse fallback by a wide margin (mean +16.13 / MAE 21.16 / corr 0.859, vs `elspot` mean +0.86 / MAE 9.02 / corr 0.954), the offset is flat across lags −4h..+4h so it is a level bias rather than a misalignment, and it was never latent: `entsoe` has hourly holes even inside files that contain it, so the fallback fires continuously. **Two reasons it stays parked.** (1) `main` explicitly decided otherwise — `d9fe03c` is titled "Label the training target's price provenance; **do not yet drop epex**", and `c0cf2a4` withheld non-ENTSO-E realised prices from *scoring* as the narrower first step. (2) The branch is **stale against a correction that landed after it**: its commit body cites 93 epex-sourced hours (1.16%), which `3545064` on `main` corrected to **141 (1.76%)** — merging as-is would reintroduce a number already known wrong. It also changes the **training target**, which is a REVIEW-class change under CLAUDE.md's Decision Framework, not something to merge as part of a wrap-up. ⚠️ It was **local-only until 2026-09-04** (on no remote, so one machine's disk was the only copy); pushed as a branch that day purely to stop that being true — pushing a branch deploys nothing, since Netlify builds `main`.
- **Two stale remote branches, deliberately NOT merged and NOT deleted (audited 2026-08-31)**: `feat/long-history-warmup` (10 commits, last 2026-04-22) and `feat/new-features-ttf-genmix` (5 commits, last 2026-04-23). Both self-report as parked — "project premise not cleanly validated", "results below gate, park and revisit" — and **`feat/new-features-ttf-genmix` has since been independently refuted**: it adds TTF gas + generation mix as features, and EXP-020 (2026-08-29) measured TTF gas as *degrading* (+3.2% QS, DM p=0.991). Merging it would ship a refuted feature. They are kept as historical evidence, not resurrected. **This is also why the ADR series has a gap at 005** — ADR-005 exists only on `feat/long-history-warmup` and was marked Paused there on 2026-04-19; it was never on `main`. `fix/use-raw-github-urls` was fully merged and deleted 2026-08-31.
- Docs structure: CLAUDE.md + docs/RUNBOOK.md + docs/decisions/ (ADR-001..008, gap at 005; ADR-001 superseded by ADR-008 on 2026-06-03) + docs/articles/ (M4 metric-redesign case study) + docs/river-arf-retrospective.md + docs/lightgbm-quantile-shadow-plan.md + docs/lightgbm-shadow-postmortem.md + docs/exp-012-results.md + docs/metric-redesign-literature-review.md + docs/literature.md + docs/hypothesis-log.md (**7 open**, none past Review by as of 2026-09-04; nearest triggers ≈2026-09-07 feed-divergence — **now part-answered, see the `load_forecast` bullet above; needs two normal-hour EDH publishes, not 7 calendar days** — and ≈2026-09-11 t0-guard. All fresh-vintage triggers slip ~5 days from the 08-31..09-04 EDH outage) + docs/experiment-backlog.md + docs/model-progress-log.md + memory/. `docs/RUNBOOK.md` gained a **Monitoring the daily job** section 2026-08-31, and its two gate-behaviour troubleshooting rows were corrected 2026-09-04 (they still described the removed 4h cap / noon-UTC floor; now the content contract and the 03:00 UTC deadline, plus the instruction to check EDH's publish-failure issue first and to read *no* issue as the worse signal). **Issues updated 2026-09-04**: `energydatahub#51` carries the full horizon evidence table; `augur#14` argues automated backfill should be **declined** (a reconstructed vintage would carry later exogenous revisions and be non-comparable in a paired log) and proposes a vintage-count helper instead; `augur#25` records that the outage is **not** an argument for an event-driven trigger, since there was no publish to dispatch on; `augur#28` and `augur#15` carry the ≈09-14 slip with thresholds unchanged.
- agent-ready-projects: v1.9.0 (hypothesis-log + literature-index patterns inform v1.10+ framework candidates).

## Recently Promoted

- **The model-promotion method** (ADR-007): pre-commit → test-on-existing-data → article-review battery → code-review battery. Each layer catches a different class of error. Promoted from the five-iteration M4 → EXP-014 arc (2026-05-29).
- **Code-review battery surfaces what article-review can't**: vintage-mismatched data joins, sort-then-pinball quantile bias, HAC-bandwidth underestimation, non-canonical metric implementations. Always fire a code-level battery before drawing conclusions from a numerical script. Promoted 2026-05-29 from EXP-013.
- **Don't condition evaluation slices on the realised outcome** (forecaster's dilemma, Lerch et al. 2017). MAE-on-y-extreme rewards constant-mean predictors and biases comparisons. Use threshold-weighted scoring (with the threshold pre-committed from a *prior* window) instead. Promoted from M4 verdict 2026-05-29.
- **Newey-West HAC bandwidth = max_horizon − 1, not `n^(1/3)`**, when paired loss differentials come from h-step-ahead overlapping forecasts. Promoted 2026-05-29 from EXP-013 code review.
- If EWM variance looks wrong → check that `ewm_mean` (signed) is used, not `ewm_abs` — promoted from code review 2026-03-28.
- If exchange prices corrupt lag buffer → ensure they're only pushed once (pre-loop), not also in forecast loop — promoted from code review 2026-03-28.
- If adding a Python dep that ships in cron → install it manually in sadalsuud's venv first; the cron does NOT run `pip install -r requirements.txt`. Caught 2026-04-30 by manual dry-run of M3 shadow pipeline (lightgbm not installed). Alternative: extend `daily_update.sh` to install deps idempotently.
- If fixing a bad path in code → also handle git state of files that lived at the bad path. M3 fixup A redirected ARF archives from `static/ml/forecasts/` to `ml/forecasts/`; the existing tracked files at the old location showed up as deletions on sadalsuud's working tree, requiring `git restore` before the branch switch could proceed. Rule: a path-fix commit should either keep the old files (they continue to be tracked, just become frozen historical) or include their `git rm` in the same commit.

## Active Decisions

- **ADR-006**: LightGBM-Quantile + CQR is the production forecasting architecture (2026-05-29). Multi-horizon stacking, 56-day rolling window, retrain-from-scratch nightly.
- **ADR-007**: Promotion method — single skill criterion + one-sided calibration guardrail, pre-committed in hypothesis-log, with article-level + code-level review batteries before action.
- **ADR-008** (2026-06-03, fully implemented 2026-06-06 in `fc24676`): Chart data carries real UTC ISO strings; **rendering layer normalises to browser-local naive** at a single boundary (`utcToLocalNaiveISO()` in `chart-renderer.js`). Supersedes ADR-001 (the `convertUTCToAmsterdam` mutation pattern caused EZ↔Augur misalignment, augur#16). The original ADR-008 claim "Plotly renders UTC strings in browser-local" was *wrong*; `fc24676` is the actual fix. CLAUDE.md hard constraint still stands: use `Intl.DateTimeFormat` for any wall-clock display; never bake offsets into stored timestamps.
- ADR-003: Netlify cache `--force` flag — ensures fresh data on webhook builds.
- ADR-004 superseded by ADR-006 (ARF replaced as model, kept as backup pipeline).
- Target: ENTSO-E NL wholesale day-ahead price + derived consumer forecast (wholesale × VAT + cached ARF surcharge).
- Features (LightGBM): 24 total — price lags (8), rolling mean/std (6), calendar (7), wind/solar/load (3) + horizon-as-feature.
- Noise: client-side `Math.random` ±5%, transparent to users.

## Open Issues

- ~~**augur#12**~~ **CLOSED 2026-08-26** (systemd migration, deployed 2026-06-08/09): system-level `augur-daily.timer` at 16:30 UTC + `ExecStartPre=wait_for_edh.sh`; timer verified `enabled` + `active` and the daily commits have landed without a gap since. Canonical units in `scripts/systemd/`; crontab deleted 2026-06-10, backup at `/tmp/crontab.backup` on sadalsuud. Rollback if ever needed: `ssh sadalsuud "crontab /tmp/crontab.backup"` + `sudo systemctl disable --now augur-daily.timer`.
<!-- verify: ssh sadalsuud "systemctl is-enabled augur-daily.timer && systemctl is-active augur-daily.timer" 2>/dev/null | grep -q "enabled" && echo PASS || { echo FAIL; exit 1; } -->
- **augur#19** (filed 2026-06-03, reframed 2026-08-25): band-coverage umbrella. **No longer a lower-side issue** — Aug 2026 is lower 0.887 / upper 0.774 / band 0.660 (target 0.80). EXP-015/016 parked; **EXP-017's premise is stale** (it targeted a high-biased q10). Re-derive the calibration bet after EXP-018a settles the feature set — the same level-drift mechanism plausibly drives both. **EXP-020 (2026-08-29) ruled out fundamentals as the fix**: adding TTF gas made QS worse and the monthly panel shows no rescue in August specifically.
- ~~**augur#26**~~ **CLOSED 2026-08-26** (ARF 48h truncation): buffer-sizing bug fixed in `f49a1c8` (`OnlineFeatureBuilder.price_history` deque maxlen 200→800 for EDH v2.2's 15-min resolution). Confirmed: `static/data/augur_forecast.json` now carries 72 forecast hours. EDH-side window narrowing remains filed as energydatahub#33.
- **augur#22** (filed 2026-06-05): HDD/CDD as long-horizon demand features. **Demoted 2026-08-25** behind EXP-018a — EXP-018 measured the three exogenous series already in the model as inert (±0.4% MAE each), so adding a fourth is a weak bet until the model demonstrably uses what it has. **Further demoted 2026-08-29** — EXP-020 tested the nearest neighbours (residual load, TTF gas) and they were inert or harmful.
- **augur#23** (filed 2026-06-06): inter-source price-chart lag investigation. After `fc24676` time-axis fix the residual lags between EZ/EPEX/ENTSO-E are smaller but still visible. Investigation tracked: all four sources publish identical `+02:00` ISO convention (ruled out as a timezone-parsing issue). Most likely causes: 15-min ENTSO-E vs hourly EPEX granularity, per-source filtering in `data-processor.js`, genuine publication-cadence differences. Low priority — likely partly genuine.
- **augur#24** (filed 2026-06-07): dashboard silent-empty-state UX hardening — 5 findings from review battery (zero-trace guard on Prices chart RPN 288, defensive shim on remaining processors RPN 252 hypothetical, DataLoader silent-null→notification RPN 210, empty-`<select>` placeholder RPN 168, `lastUpdate` `new Date()` fallback lies RPN 120). No FAIL; all in the same silent-failure category that the `4a557c8` fix patched defensively but didn't generalize. Recommend tackling #1/#3/#4/#5 (real pre-existing modes); skip #2 (hypothetical future schema bump).
- **EDH-side follow-ups** — filed 2026-06-12 as **energydatahub#34** (elspot: today-only fetch + probable ENTSO-E redundancy since the Feb 2026 pynordpool migration) and **energydatahub#33** (v2.2 load/price file window narrowed to today+tomorrow; also stale `resolution: hourly` metadata on 15-min data). If #34 resolves by retiring elspot, drop it from `static/js/modules/constants.js` DATA_SOURCES.forecast.
- **augur#18** (filed 2026-06-03): verify EnergyZero endpoint really returns all-in consumer pricing as the constants.js comment claims; clarify wholesale vs consumer comparator labelling on the dashboard.
- **augur#15 / EXP-021a** (filed 2026-06-03): foundation-model spike. **First arm run 2026-08-29 and it won decisively** (EXP-021, above). The old note "won't fix the calibration gap" is **wrong and now retired** — the calibration result is the strongest half: raw band coverage 0.811 vs the incumbent's 0.611, with no conformal layer. Next: EXP-021a Stage 1 on fresh vintages ≈2026-09-09, *after* EXP-018a Stage 1 has claimed that window.
- **augur#14**: gap-detection + automated backfill for missed daily runs. **Detection half shipped 2026-08-28** (t0-advance alarm); the backfill half is an open design question — a vintage reconstructed from fresher exogenous is not comparable with the live ones beside it in `eval_log.jsonl`, so "leave the hole" may be the right answer. See the 2026-08-28 comment. **2026-06-12 comment** documents the concrete incident (three "ARF OK | eval rc=0" commits while ARF published empty forecasts; eval vintages 06-08/06-10 permanently unevaluable); the two `daily_update.sh` post-run guards (`1c33daa`) cover the alarm half — automated backfill remains open.
- **Model-tab metric parity**: `update_shadow.py` doesn't yet emit ARF-equivalent metadata (`metrics_history`, `error_history`, `n_training_samples`), so `static/js/modules/model-viz.js` still reads `augur_forecast.json` (ARF backup). Future work to extend the LightGBM metadata schema and update model-viz.js.
- **GPU compute (re-probed 2026-08-29)**: `b650-gpu` (16 cores, 30 GB, RTX 3090 Ti 24 GB) and `gpu-server` (16 cores, 48 GB, RTX 4080 16 GB) are reachable **via the `~/.ssh/config` aliases** — connecting as `user@hostname` with a guessed username fails `publickey,password` and looks like the host is down. Neither has `lightgbm`/`pandas` installed. Both are 16 cores like situla, so they add nothing for CPU-bound LightGBM sweeps; they matter only for the GPU-shaped track (augur#15 foundation models). `sadaltager` offline (16d).
<!-- verify: if ssh -o BatchMode=yes -o ConnectTimeout=6 b650-gpu true 2>/dev/null; then echo "b650-gpu reachable by alias"; else echo "CANNOT VERIFY: b650-gpu unreachable"; fi -->
### Standing conclusions after EXP-021..031 (2026-08-30) — orient here before proposing model work

**Settled, do not re-litigate** (each closed by a test that could have refuted it; ADR-007 forbids re-running a refuted position with a looser Method — a new bet needs a new Position and a mechanism explaining why the earlier test could not have seen the effect):
- **Feature engineering is closed for LightGBM at 56 days.** Adding exogenous columns buys nothing (EXP-018/020); adding *level* columns actively costs — confirmed four independent ways: internal rolling mean (EXP-019), exogenous gas (EXP-020), residual-regression level columns (EXP-029), and matched-count derived columns (EXP-024).
- **The FM's context advantage is not recoverable inside LightGBM.** Widening the 14-number feature row fails (EXP-024, +2.5% worse). The bottleneck is model class, not feature-row width.
- **Do not fine-tune Chronos on this asset.** Both skill and calibration degrade (EXP-027: MAE +19%, coverage 0.825→0.394). Only ~3.6k pre-cutoff points exist. Zero-shot is the deployment mode. *Caveat: naive recipe, no early stopping — a careful recipe was never tried.*
- **Model size is not a constraint, and neither is latency.** tiny retains 93.5% (EXP-026); base runs a 72h forecast in 0.52s on 4 threads (EXP-030). Deployment cost is the torch dependency, not compute.

**Live decisions, in order of cheapness** (revisit triggers + review-by dates in `docs/hypothesis-log.md`):
1. **Chronos-2 covariates** (EXP-028, `parked`) — +8.3% QS, first positive exogenous result; confirmation pre-committed as **EXP-028a** (≥28 fresh vintages ≈2026-09-23), which is also the only thing that resolves the contamination exposure by construction.
2. **112-day window** (EXP-023, `parked`) — **demoted 2026-08-30**: EXP-023a Stage A (EXP-032) found the advantage does *not* reproduce on 56 unscored vintages — 112d is **1.4% worse**, DM p=0.894, sign flipped from the discovery's −3.0%. Alternative 1 (regime artifact) fires: 112d wins in the May–Aug rising-price stretch, loses in calmer Mar–May. Stage A is a *quasi*-holdout so this does not formally refute EXP-023, but it is no longer a cheap near-certain win. Stage B (≥45 fresh vintages ≈2026-10-09) is now the real test.
2. **Chronos-2 covariates** (EXP-028, `parked`) — +8.3% QS, Augur's first positive exogenous result, but fails the coverage gate and contamination is not excluded by construction (weights modified 2026-06-05, inside the window).
3. **EXP-021a** — the confirmation ladder for the foundation model: fresh vintages → feasibility → live shadow → only then an ADR-006 amendment.

**Numbers not to requote:** the FM's calibration edge is **0.811 vs lean+CQR 0.788**, *not* vs the pre-conformal 0.611 (EXP-025). And EXP-021's stated mechanism (level drift) was **wrong** — it is a pretrained distributional prior plus a feature-row information bottleneck (EXP-022).

- **Experiment results are readable at `docs/experiment-results.md`** (2026-08-30) — opens with a **Decision state** section (closed / open / standing evidence, derived mechanically from `decision` so it cannot drift), then summary table + per-experiment hypothesis/outcome/metrics/caveats for all 30 entries. **Generated** by `scripts/render_results.py` from `registry.jsonl` + the `summary.json` artifacts, so it cannot drift; `--check` exits non-zero if stale. Added because the registry is JSONL — a good source of truth, unreadable as documentation.
- **Registry integrity is now checkable, not asserted (EXP-031, 2026-08-30)**: `scripts/audit_registry.py` runs 5 checks — schema, id order, artifact existence, **number traceability to a committed artifact**, and a **sha256 proof that all 7 pre-committed Method bodies are byte-identical between `4024420` and HEAD**. Exits non-zero; run at `/curate`. It found one real defect: 19 of 241 numeric metrics were untraceable (16 from throwaway inline analyses never saved — the same failure as leaving results in /tmp). Fixed by `scripts/posthoc_diagnostics.py`; now 0 of 241. Two **pre-existing** issues surfaced, not introduced and not fixed: 8 older entries have empty `commits[]`, and 5 annotate artifact paths as `file.md (note)`.
- **Experiment backlog (`docs/experiment-backlog.md`, written 2026-08-29)**: five designed, pre-committed, unrun experiments descending from EXP-021/022 — **EXP-025** transplant Chronos's spread onto the incumbent's median (minutes, CPU, directly serves augur#19, *do first*), **EXP-023** window-length sweep 28/56/84/112/168d (~3h CPU, the lever EXP-020 named and nobody pulled), **EXP-024** raw-lag richness with a dimensionality-matched derived-column control (~1h CPU, sharpens EXP-018 from "fewer features" into "fewer *derived* features"), **EXP-026** bolt-tiny/mini/small/base ladder + sadalsuud CPU latency bench (de-risks EXP-021a Stage 2), **EXP-027** fine-tuning predicted to improve MAE *and* destroy calibration (a dissociation, GPU hours, should follow EXP-021a Stage 1). All run on the existing 260-vintage window and **consume no fresh vintages**, so none collide with EXP-018a Stage 1 (≈2026-09-09) or EXP-021a.
- **Publishability backlog** (`docs/hypothesis-log.md` entry, review-by 2026-12-31): ADR-006 + the M4 → EXP-014 arc is publishable with ~2-3 weeks of empirical follow-up (naive baseline, PIT, multi-window robustness, canonical CRPS at 9-19 quantiles, canonical twCRPS integral). Or ~3-4 days of polish for a blog post.
- **Deferred ML features**: **#3 (gas/carbon) is CLOSED 2026-08-29** — EXP-020 tested TTF gas and it *degrades* (+3.2% QS, DM p=0.991). #2 (NED production), #4 (cross-border flows) and #22 (HDD/CDD) stay open but are further demoted: EXP-020 also found residual load inert (p=0.648 / p=0.899 on two windows) and plain `load_forecast` inert (p=0.851). The parsers are no longer banked on a branch — `wind_gen_forecast_mw`, `solar_gen_forecast_mw`, `gas_ttf_eur_mwh` and `is_holiday_nl` landed on `main` in `ml/data/consolidate.py` and are in the parquet, just not in `FEATURE_COLUMNS`. Revisit only if the window length or model class changes (augur#15).
- **`weather_forecast_multi_location.json`** (consumed by augur dashboard Weather tab): Open-Meteo since 2026-06-05 (was Google Weather; collector swap in energydatahub `df1bdb8`/`6e9433e`). 6 strategic CWE+DK locations (Hamburg/Munich/Arnhem/IJmuiden/Brussels/Esbjerg), 10-day forecast, 17 fields incl. WMO `condition` text via energydatahub `WMO_CODE_MAP`.
- **`demand_weather_forecast.json`** (NOT yet consumed; tracked at augur#20 for dashboard wire-up + augur#22 for model-feature wire-up): Open-Meteo at 11 NL/DE/BE population centers, 7-day forecast, includes pre-computed HDD/CDD.
- **`weather_forecast_buurt.json`** (NOT consumed by augur; FyE B1 input): Open-Meteo at Elsweide + Elderveld centroids (computed from CBS Wijken en Buurten polygons), 16-day forecast, full enriched field set.
- **`solar_forecast_buurt.json`** + **`air_quality_buurt.json`** (NOT consumed by augur; FyE B1 inputs): Open-Meteo solar irradiance + Luchtmeetnet (RIVM) NO2/PM10/PM25/O3/AQI at the same buurt centroids.
- **Workflow defensive gate**: `energydatahub/.github/workflows/collect-data.yml` now exits 1 when `data_quality_report.json:overall_status == "critical"`. Prevents the silent-failure pattern that masked GoogleWeatherCollector returning `API_KEY_INVALID` for ~7 months (Nov 2025 → Jun 2026).
- **Storage migration**: energydatahub repo uses git as time-series archive (intentional). Tracked at energydatahub#9 — runway ~17 months until hits GitHub's 1 GB soft limit.
- **Product expansion**: augur#8 (SaaS API), #9 (ensemble forecasting — overlaps with #15), #10 (multi-country). Strategic / long-horizon.
- **agent-ready-projects#12** (framework-level, not augur): calendar-bridge skill candidate — plant `Review by:` dates from hypothesis logs into Google Calendar.

## Closed 2026-08-28

- ✅ **Silent vintage loss** — t0 stall/jump now alarms at the step that owns t0 (`4a2afc4`), and `wait_for_edh.sh` no longer releases the run on an overnight catch-up publish (`05b4d43`). Deployed to sadalsuud the same day; first observation is the 2026-08-28 18:30 CEST run. Position + 14-run review in `docs/hypothesis-log.md` [2026-08-28].
- ✅ **2026-08-27 `[ALARM: eval stale 3d]` diagnosed** — not an evaluator fault. `eval_log.jsonl` ended 08-24 because 08-25's vintage was never created (t0 jumped 08-24T21 → 08-26T21) and 08-26 sat at 23 of the 24 realised hours it needs. The 08-24 trigger was ours: EDH's 06:28 catch-up satisfied the date-only gate and the real publish landed 16:32:17, 90 seconds after Augur finished.
- ✅ **energydatahub#50 filed** — EDH's scheduled publish silently skips whole days; quantified over 35 days.

## Closed 2026-06-03

- ✅ augur#16 — dashboard timezone-mutation bug (ADR-001 superseded by ADR-008; convertUTCToAmsterdam removed). Commit `5ae82b4`.
- ✅ augur#17 — dashboard wholesale lower-band clamp at 0 (hid LGBM's negative-price predictions; same pattern as deprecated ARF clamp). Commit `07fb9a4`.
- ✅ augur#5 — backtesting framework substantially completed in a different shape during EXP-009..014.
- ✅ augur#6 — peak/off-peak model variants speculative + obsolete (LGBM already does horizon stacking).
- ✅ augur#7 — ARF ensemble / Prophet baseline stale (ARF retired; Prophet overlaps with #15).
- ✅ augur#11 — cron→systemd standalone superseded by broader augur#12.

## Closed 2026-06-12

- ✅ **Missing eval-row forensics** — vintages 06-08/06-10 explained (EDH v2.2 → stale parquet → t0 froze/jumped; permanent, unrecoverable); 06-09 `arf_mae: null` explained (empty ARF archive, no lookup fallback). Incident on augur#14.
- ✅ **Post-run output guards in `daily_update.sh`** (`1c33daa`) — ARF forecast <24h + eval row stale >2d alarm in commit subject. Pattern promoted: "rc=0 is not output quality".
- ✅ **EXP-015 (per-side CQR replay)** — parked; pre-commit `bcc3e78`, resolution `b40db95`. Fixes side asymmetry; can't reach regime days. Baseline redirect: horizon-conditioning refuted (deficit flat across groups).
- ✅ **EXP-016 (per-side ACI replay)** — parked; pre-commit `440b0b6`, resolution `d21b179`. Fixes post-shift days; γ-independent ~0.85 ceiling from first-shift days; Winkler guardrail tripped. Arc conclusion: gap is in the raw quantiles → EXP-017 next.
- ✅ **augur#26 filed** — ARF 48h truncation (EDH v2.2 file-window narrowing), live degradation, EDH-side fix preferred.

## Closed 2026-06-10

- ✅ **EDH v2.2 envelope wrap Python parser fix** — `e11487b` adds `_unwrap_v22_envelope` shim to `ml/data/consolidate.py`. Parquet recovered from being pinned at 2026-06-07 21:00Z; full 72h dashboard forecast restored. Eval log backfilled for 2026-06-07 (LGBM MAE 30.6 vs ARF 39.5) and 2026-06-09 (LGBM 19.9, ARF empty). 2026-06-08 has a permanent eval-log gap (no LGBM prediction set was made for that target date during the outage). Code-review battery returned REVIEW with 4 non-blocking findings (parser test gap is biggest); filed for separate follow-up.
- ✅ **augur#12 cron-comment cleanup** — sadalsuud crontab now empty; systemd timer is the sole trigger. Backup at `/tmp/crontab.backup`.
- ✅ **sadalsuud `.venv/` untracked** — `d20992a` adds `.venv/`+`venv/` to `.gitignore`; `967b653` runs `git rm -r --cached .venv/` (7921 files untracked). Next daily commit (`576a65c`) was clean — only 6 data/state files instead of swept-up venv noise.
- ✅ **sadalsuud lightgbm reinstall** — install was silently corrupted at 15:50 UTC today (root cause unknown — directory existed but contained no `__init__.py`); `pip install --force-reinstall lightgbm` recovered. Gotcha logged for potential pre-flight hardening.

## Closed 2026-06-09

- ✅ Healthchecks.io shadow endpoint removed from `scripts/daily_update.sh` (curl ping block deleted) and sadalsuud `.env` (`HEALTHCHECKS_SHADOW_URL` line removed). Comments in `scripts/wait_for_edh.sh` + `scripts/systemd/README.md` reworded — the alarm path is now: pre-flight `SHADOW_PRE_AGE_H >36h` ALARM in `daily_update.sh` surfaces stale state in the next commit message, and absence of a daily commit on origin/main is the external alive signal. Orphaned hc-ping UUID `e7771ae1-…1cc58bab0992` left dangling on healthchecks.io (owning account unknown; check will go silent/down on its own since no pings are sent).

## Resolved 2026-05-29

- ✅ EXP-011: M4 verdict (PROMOTE=False initially, Path B park).
- ✅ EXP-012: metric-redesign validation on existing data — surprise findings.
- ✅ EXP-013: corrections following code-review battery (vintage-join bug; pinball-at-p10 reversed).
- ✅ EXP-014: redesigned-criterion pass + LightGBM promoted to production (Path A swap).
- ✅ Article draft: `docs/articles/m4-metric-redesign-story.md` (five-iteration arc).
- ✅ Literature bibliography: `docs/literature.md`, `docs/metric-redesign-literature-review.md`.
- ✅ ADR-006 and ADR-007 written.
- ✅ `tests/test_metrics.py` (19 tests) added.
