# Upstream: energyDataHub

<!-- Load when: an EDH publish is late/short/missing, a feed changes shape or span,
     or you are about to cite an energydatahub issue number.
     The one-line state lives in memory/MEMORY.md; this file is the depth. -->

The lessons from these incidents are in `memory/gotcha-log.md` (2026-08-31 and
2026-09-04 entries). This file is the *upstream relationship* record: what the
feeds do, who detects what, and which issue numbers are real.

## Division of labour — read this before trusting either side's alarm

**Augur's `t0` hold-back and heartbeat detect the ABSENCE of a fresh vintage;
EDH's issue explains a run that fired and FAILED.** Ours covers the worse case.

⚠️ EDH's publish-failure alerting is **DIAGNOSIS, NOT DETECTION. Do not make it
the primary alarm.** (An earlier note had this hierarchy backwards.) Its `alert:`
job lives *inside* `energydatahub/.github/workflows/collect-data.yml` with
`needs: [collect-and-publish, deploy]`, and that workflow's only triggers are
`schedule` and `workflow_dispatch` — verified on main. It is therefore
structurally blind to a run that never starts: a dropped cron (GitHub
deprioritises schedules on quiet repos), a queue outage, or a disabled workflow
all produce no run, no alert, and a clean issue tracker while nothing publishes.

⚠️ **Our alarm firing with NO issue on `ducroq/energydatahub` is a WORSE signal
than an issue existing** — it means the pipeline is not running at all, rather
than running and failing. For the 08-31..09-03 outage theirs would have fired on
all five days; had the cron simply stopped, theirs would have been silent on all
five and only ours would have caught it. A true liveness check must fire when a
publish has *not* happened for N hours and cannot live inside the workflow it
watches — unbuilt on both sides, and the same shape as Augur's own accepted blind
spot (the heartbeat runs *on* sadalsuud, so it cannot report sadalsuud down).

## Issue-number map — verified against the API 2026-09-04

| Number | What it actually is |
|---|---|
| `energydatahub#47` | member-drift duration guard |
| `energydatahub#50` | skipped publishes ("silently skips whole days") — **live and correct, do not strike** |
| `energydatahub#51` | load-forecast horizon — closed as **resolved-upstream**, not transient |
| *(none)* | the abandoned time-boxed drift block — **it has no number, because it does not exist** |
| `energydatahub#69` | publish-failure issue for the 09-07 outage — opened 09-07 19:51Z, auto-closed 09-08 19:16Z |
| `energydatahub#70` | publish-failure issue for the 09-09 outage — opened 09-09 19:13Z, auto-closed 09-10 ~07:4xZ |
| `energydatahub#71` | Luchtmeetnet `air_quality_buurt` missing (HTTP 429) — **Augur does not consume it** |
| `energydatahub#73` | whether `data/data_quality_report.json` should be a *published* artifact — **read this before adding gating** |

⚠️ The time-box was cited as `#47`, then as `#50`, and neither is its number.
Citing any number for it is how a phantom dependency gets recorded.

## Publish reliability

EDH published on **31 of 35 days** over 2026-07-25..08-28 (missed 08-03, 08-06,
08-23, 08-27; catch-up double-publishes 08-09, 08-24) — roughly an 11% miss rate.
Augur's gate is fail-open by design, so each miss costs a vintage and now
announces itself. Filed as **energydatahub#50**.
<!-- verify: n=$(gh api "repos/ducroq/energyDataHub/commits?path=data/data_quality_report.json&per_page=100&since=2026-07-25T00:00:00Z&until=2026-08-29T00:00:00Z" --jq ".[].commit.committer.date" 2>/dev/null | cut -c1-10 | sort -u | wc -l); if [ -z "$n" ] || [ "$n" = 0 ]; then echo "CANNOT VERIFY: gh api unreachable or empty"; else echo "EDH published on $n of 35 days in 2026-07-25..08-28 (claim says 31)"; [ "$n" = 31 ] || exit 1; fi -->

## Publish outage 2026-08-31..09-04 — five vintages lost, upstream, resolved

EDH's "Collect and Publish Data" failed on every run from 08-31 to 09-03, last good publish 2026-08-30T19:03. Cause: its **schema-drift tripwire deadlocked**. The tripwire (`energydatahub/.github/workflows/collect-data.yml:153`) runs *before* the `Update energy data` commit (`:183`) and compares against `--previous-ref HEAD`, but `data/_shape_signatures.json` is only written *by* that commit — so the baseline froze at 08-30 and the gate was guaranteed to re-fire, with a different offending feed each run as the gap widened. Only `Record shape observation` commits landed, which made the repo history look alive while publishing nothing. **Augur behaved correctly throughout**: the t0 guard, the hold-back alarm and the heartbeat all fired, the heartbeat emailed on day 1 and then deduplicated by shape, and on 09-04 the new content-contract gate **refused a pre-auction vintage** (dispatched 06:53 UTC, `entsoe` at 96 points vs a 192 median — the day-ahead auction clears ~12:00 CET). No corrupt data entered the parquet. **The fix is EDH-side and was theirs to make.** A time-boxed drift block (block at most 3 consecutive runs, then relent, baseline still only advancing on a pass) was implemented and then **abandoned, not shipped** — their review found its only reachable domain was `CRITICAL_FEEDS` + `MEMBER_MAPPED_FEEDS`, the two sets that must never be waved through, and that it would have relented on **zero of the five** runs this outage actually produced. What landed instead is **energydatahub PR #64, merged as `a5993e7`** — publish-failure alerting only, plus tests that run when `scripts/` changes and a manual self-check (`a96a7f7`). **NEW UPSTREAM SIGNAL — it is DIAGNOSIS, NOT DETECTION. Do not make it the primary alarm** (an earlier version of this bullet said "use it before inferring from our own alarms", which had the hierarchy backwards and is corrected here). A failing EDH publish opens an issue titled *"Daily publish is failing"* on `ducroq/energydatahub` carrying the run URL and both job results, comments rather than duplicating on repeat failures, and closes itself on recovery. But `alert:` is a **job inside `energydatahub/.github/workflows/collect-data.yml`** with `needs: [collect-and-publish, deploy]`, and that workflow's only triggers are `schedule` and `workflow_dispatch` — verified on main. **So it is structurally blind to a run that never starts**: a dropped cron (GitHub deprioritises schedules on quiet repos), a queue outage, or a disabled workflow all produce no run, therefore no alert, therefore a clean issue tracker while nothing publishes. Correct division: **Augur's `t0` hold-back and heartbeat detect the ABSENCE of a fresh vintage; EDH's issue explains a run that fired and FAILED.** Ours covers the worse case. ⚠️ **Our alarm firing with NO issue on `ducroq/energydatahub` is a worse signal than an issue existing** — it means the pipeline is not running at all, rather than running and failing. For the 08-31..09-03 outage theirs would have fired on all five days; had the cron simply stopped, theirs would have been silent on all five and only ours would have caught it. A true liveness check must fire when a publish has *not* happened for N hours and **cannot live inside the workflow it watches** — unbuilt on both sides, and the same shape as Augur's own accepted blind spot (the heartbeat runs *on* sadalsuud so it cannot report sadalsuud down). Validated on a real runner with the write-scoped credential (the create path needs issues-write where the close path needs only read — a PAT sufficient for the daily run could still have been unable to raise the alarm, discoverable only mid-outage). ⚠️ **`wind_forecast` → `CRITICAL_FEEDS` did NOT ship** — I recorded it as part of #64 on 2026-09-04 and that was wrong; verified against the API, `CRITICAL_FEEDS` on main is still the original four. It sits committed-but-unmerged on branch `critical-feeds-wind-forecast`, held back deliberately: it makes the gate stricter (a shape excursion would block a publish where it now warns, ~1 run/month measured) and shipping something that can cost a publish while nobody is watching was the wrong trade. So the span/structure gap is documented and **open on both sides**, not quietly closed. ⚠️ **Issue-number hygiene (2026-09-04), and note the scope — this is about the TIME-BOX only:** the time-box was cited as `#47`, then as `#50`, and **neither is its number**; it has none, because it does not exist. Verified against the API: `#47` = member-drift duration guard, `#50` = skipped-publishes ("silently skips whole days"), `#51` = load-horizon. **`#50` remains a correct and live reference** — it is the issue this index has tracked since 08-28 *and* the one PR #64's publish-failure alerting addresses, so do not strike it anywhere. Citing any number for the abandoned time-box is how a phantom dependency gets recorded. ⚠️ **Do not "fix" this by moving Augur's gate or proposing EDH move theirs after the commit** — `energydatahub/.github/workflows/collect-data.yml:119-121` states the invariant: the baseline "must only advance on a passing run, or a broken shape becomes the new normal". Moving it would have adopted SE-less `nordic_hydro` and 10-of-11-location `demand_weather_forecast` as normal, i.e. silent permanent data loss into Augur — strictly worse than a visible gap. **Schedule cost: every fresh-vintage-gated trigger slips ~5 days** — EXP-018a Stage 1 / EXP-021a from ≈09-09, and the feed-divergence review from ≈09-07.

## `load_forecast` horizon halved 2026-08-28, recovered 2026-09-04 — NOT structural

decrypted every `load_forecast` vintage on EDH `origin/main` and counted **distinct calendar days** as well as points, so a resolution change cannot be mistaken for a horizon change. Normal is **192 pts / 2 days** for months through 08-26; **96 pts / 1 day** on 08-28, 08-29, 08-30 and 09-04. `metadata.end_time` still declares +2 days throughout, so the envelope over-declares its own coverage and EDH's request window is unchanged — this is **upstream ENTSO-E A65 availability**. **RESOLVED 2026-09-04 on the load side — both zones recovered; see `docs/hypothesis-log.md` for the full table.** EDH's `260904_081356` publish (08:14 UTC) carries **load NL 192/2 days AND DE_LU 192/2 days**. A direct A65 probe 15 minutes earlier (09:59 CEST) had shown DE_LU still at 96, which **confirms the publication-timing effect** — German day-ahead load lands later in the day than Dutch, so a morning DE_LU short reading is benign. The 08-28..08-30 episode was nonetheless a **genuine both-zone A65 gap**, not timing: `260830_190255` was short at 19:02, far later than today's ~10:14 recovery. Net: a real multi-day upstream gap that healed, costing three vintages of reach — so **energydatahub#51 closes as resolved-upstream, not as transient**, and the "new steady state" reading is falsified *before its pre-committed signal could accumulate* (it needed load to stay at 96; load is back at 192). ⚠️ Price at 96 in both of today's publishes is **not** the divergence — both are pre-auction (the NL day-ahead clears ~12:00 CET). **This is an NL-only question for Augur**: `consolidate.py:320 parse_load_file` reads `load_data.get("NL", {})` hard-coded, and `grep -rn "DE_LU" ml/ static/js/ scripts/` returns zero hits repo-wide — DE_LU is structurally invisible here, so do not spend time on the German half. ⚠️ **Two wrong readings to not repeat, both held during the 09-04 session**: "load_forecast is a same-day feed, 96 quarter-hours by construction, so the 18h hold-back is permanent and should be designed around" (refuted — it carried 192 for months), and the mirror error of generalising a structural shape from a *single* vintage. The discriminator both readings missed is **publish hour**: every pre-08-28 short vintage is an early-hour run that recovered to 192 on the same day's normal-hour run (08-09, 08-24), so short-at-early-hour is benign and normal-hour-short is the signal. **`260830_190255` at 19:02 is the lone unambiguous normal-hour short vintage**, which is why the feed-divergence hypothesis's Alternative 1 ("new steady state", signal = 3 consecutive normal-hour publishes at price 192 / load 96) is **strongly indicated but formally short** — the outage denied us the normal-hour publishes the test needs. Resolve on the next two scheduled EDH runs. **energydatahub#51 must not be closed as transient on this evidence.** Standing gap, told to EDH and unowned by either side: `load_forecast` is in their `CRITICAL_FEEDS` and delivered half its horizon for four publishes without their gate objecting, because **a shape signature captures structure, not span** — 96 and 192 records have identical shape. Augur's t0 hold-back alarm was the only thing that noticed.
<!-- verify: grep -q "t0_short_feeds" ml/shadow/update_shadow.py && grep -q "t0 held back" scripts/daily_update.sh && echo "hold-back alarm still the only span detector on this side" || exit 1 -->

## Publish outage 2026-09-07 and 2026-09-09 — two more vintages lost, TWO UNRELATED CAUSES

Last good publish before the gap: **2026-09-08T19:15:52Z** (`da5eab2`). Recovered by a *dispatched* run at **2026-09-10T07:41:04Z**. Confirmed with the energyDataHub session rather than inferred from the artifact — the two failures are unrelated and only one is upstream:

- **09-07 (run 34155885124) — genuinely ENTSO-E.** Failed in *Collect data*: an ENTSO-E outage opened their shared per-host breaker, which suppressed 103 sub-requests; `entsoe` and `entsoe_de` were still missing after 3 retry rounds and the run exited 1. Self-healed by 09-08.
- **09-09 (run 34392331572) — NOT ENTSO-E, and theirs.** Prices collected fine. **NED.nl timed out on all six of its fetches inside 50 seconds; `collectors/ned.py` logs and swallows each one and still returns a truthy envelope with `data={}`.** Their completeness gate scored that CRITICAL on 0 points and **aborted the publish of all 19 healthy feeds** — including the `entsoe` Augur was waiting on. A `ned_production` grace (empty envelope treated as absent, non-blocking, for up to 3 consecutive runs) is now deployed, **but NED recovered on its own that run, so the coercion path has never executed in production — treat it as untested, not proven.**

**No backfill exists.** On 09-09 the data was collected but never committed, because the gate aborts before the publish step. `scripts/backfill_entsoe.py` reads the pre-v2.2 envelope and would write malformed files (their #57). So 09-07 and 09-09 are permanently gone, and Augur's `t0` jumps rather than filling in — the "loses a vintage differently" case.

**Do not misread the cadence on 2026-09-10.** There are **two** publishes that day: the 07:41Z dispatched recovery, and the ordinary scheduled run landing ~17:50–19:30Z. Two publishes in one day is unusual for EDH and is not a duplicate; the gate's strictly-newer contract simply advances twice, on consecutive days.

⚠️ **The 07:41Z recovery carries `entsoe=96` — same-day only, pre-auction.** This is the same shape as the publish the gate correctly refused on 2026-09-04, but our `median_points` expectation has since decayed to 96 and will now **accept** it. See `docs/hypothesis-log.md` [2026-09-10]; this is a live gap, not a historical note.

## Augur now reads the `publish-failure` label — and this does NOT contradict "diagnosis, not detection"

`scripts/wait_for_edh.sh` polls EDH's open `publish-failure` issues (unauthenticated `api.github.com`, python3 stdlib — **not `gh`, which is not installed on sadalsuud**) and gives up its wait early when one was opened *after this run started*. The hierarchy recorded above is unchanged and still correct: **Augur's `t0` hold-back and heartbeat detect the ABSENCE of a fresh vintage; EDH's issue explains a run that fired and FAILED.** The probe cannot start or extend a wait and is never a primary alarm — it only shortens a wait we were going to abandon anyway, and every way of not knowing falls through to the existing deadline. The structural blindness noted above (a run that never starts opens no issue) is exactly why it stays secondary.

**Known limitation:** EDH opens **one** issue per failure *streak* and closes it on recovery. #70 sat open from 09-09 with `updatedAt == createdAt` and zero comments, so on the second and later nights of a streak nothing is newer than our start time and the gate waits to the deadline as before. The probe helps on the **first** night — when a still-evaluable vintage is at risk — and not after. Making it work every night requires EDH to touch the issue per repeat failure; asked, not yet answered.

⚠️ **Dependency worth knowing before building more gating on it (their #73):** `data_quality_report.json` is committed to `data/` only and is **not** copied into `docs/`, so it is not part of the Pages artifact and carries **no published-schema contract**. Augur reads it from the git checkout (`git show origin/main:data/data_quality_report.json` in `wait_for_edh.sh`), which works, but it is a file upstream has never treated as published and may reshape without notice. Two existing cautions still apply: **do not gate on `_span_shortfalls.json`** (their #53 — reporting-only, no expectations yet; the last two runs reported "0 of 56/64 members have >=10 prior observations", so it cannot return a meaningful clean result), and **never judge feed freshness by file mtime** — a graced feed is *absent* from a publish, so `docs/` keeps yesterday's file unchanged and it looks fresh while being a day stale.
