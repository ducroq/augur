# Dev/test-only dependencies — NOT needed for the Netlify build or the
# daily ML job's happy path, but required to run the pytest suite (the
# smoke gate in scripts/daily_update.sh and the bootstrap verification).
#
# Install alongside the runtime env:  scripts/bootstrap_venv.sh --dev
# NOTE: pytest is intentionally NOT in requirements.lock — that lock is the
# runtime closure only (pip freeze --exclude-editable). Dev tooling is
# pinned loosely here and never affects production output; keep it out of
# the runtime lock so deploy installs stay minimal and reproducible.
pytest>=8.0
